description: Internal server error
content:
  application/json:
    schema:
      $ref: './../schemas/responses/error-response.yaml'
    examples:
      internalError:
        $ref: './../examples/internal-error.yaml'