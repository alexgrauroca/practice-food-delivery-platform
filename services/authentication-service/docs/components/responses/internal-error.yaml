description: Internal server error
content:
  application/json:
    schema:
      $ref: './../schemas/error-response.yaml'
    examples:
      internalError:
        $ref: './../examples/internal-error.yaml'