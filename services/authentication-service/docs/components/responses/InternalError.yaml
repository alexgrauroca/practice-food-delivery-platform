description: Internal server error
content:
  application/json:
    schema:
      $ref: './../schemas/ErrorResponse.yaml'
    examples:
      internalError:
        $ref: './../examples/InternalError.yaml'